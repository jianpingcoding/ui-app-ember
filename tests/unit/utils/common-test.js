import common from 'web-ember-app/utils/common';
import { module, test } from 'qunit';

module('Unit | Utility | common', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = common();
    assert.ok(result);
  });
});
